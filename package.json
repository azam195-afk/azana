{
  "name": "azana-static-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static production build for AZANA DESIGN.",
  "scripts": {
    "build": "node scripts/build-static-site.mjs",
    "preview": "npm run build && python3 -m http.server 4173 --directory dist/site"
  },
  "engines": {
    "node": ">=18"
  }
}
